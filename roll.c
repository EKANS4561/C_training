#include <stdio.h>
void main ()
{
    int age;
    float h;
    printf("enter age :");
    scanf("%f",&age)
    printf("enter h :");
    scanf("%f",h);
    if ((age>12) && (h>4.5))// 13>12 ? T and 5<4.5? T ==> T
    {
        printf("you can ride roller coaster");
       }
{
    printf("grow up and come back!");
}
}